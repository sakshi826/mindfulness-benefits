server {
    listen 80;

    location /mindfulness-benefits {
        alias /usr/share/nginx/html/mindfulness-benefits;
        index index.html index.htm;
        try_files $uri $uri/ /mindfulness-benefits/index.html;
    }

    location = / {
        return 301 /mindfulness-benefits/;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public";
    }
}